import type { Theme } from "../types";

export const opincodeDefault: Theme = {
  id: "opincode-default",
  name: "OpinCode Default",
  description: "The default OpinCode look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
