# OpinCode v0.0.3

This release tightens up the macOS packaging path. The release workflow now carries the Apple signing and notarization keys through the runner, so Apple Silicon and Intel builds should ship signed instead of tripping Gatekeeper on first open.

I also kept the version bump flow tied to `main`, so matching version files still turn into a tag and a published release automatically.
